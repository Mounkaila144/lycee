# Story 4.4: Reconduction des Affectations

## Status: Aproved

## Story

**As an** Admin,
**I want** dupliquer les affectations d'une année scolaire vers une nouvelle année,
**so that** je puisse reconduire les affectations existantes sans tout ressaisir.

## Acceptance Criteria

1. Un bouton "Reconduire les affectations" est disponible sur l'écran d'affectation
2. Le clic ouvre un formulaire : Année source, Année cible
3. Le système copie les affectations et les mappe sur les classes de l'année cible (même niveau/série/section)
4. Un rapport de reconduction est affiché : X reconduites, Y non reconduites (avec raison)
5. L'Admin peut ajuster manuellement les affectations non reconduites
6. La reconduction ne crée pas de doublons

## Tasks / Subtasks

- [ ] Task 1: Créer l'endpoint de reconduction (AC: 1-6)
  - [ ] `POST /api/admin/teacher-assignments/carry-over`
  - [ ] Body: source_academic_year_id, target_academic_year_id

- [ ] Task 2: Implémenter la logique de mapping (AC: 3, 4)
  - [ ] Pour chaque affectation source, trouver la classe cible avec le même (level_id, series_id, section) dans l'année cible
  - [ ] Créer l'affectation si la classe cible existe et pas de doublon
  - [ ] Collecter les affectations non reconduites avec raison

- [ ] Task 3: Créer CarryOverAssignmentsRequest (AC: 2)
  - [ ] source_academic_year_id (required|exists)
  - [ ] target_academic_year_id (required|exists|different:source)

- [ ] Task 4: Tests (AC: 1-6)
  - [ ] Test reconduction réussie
  - [ ] Test classe cible manquante
  - [ ] Test pas de doublons créés

## Dev Notes

### Dépendances
- Story 4.1, Story 2.3

### Logique de Mapping
```php
$sourceAssignments = TeacherSubjectAssignment::where('academic_year_id', $sourceYearId)->with('class')->get();
$report = ['created' => 0, 'skipped' => []];

foreach ($sourceAssignments as $assignment) {
    $targetClass = Classe::where('academic_year_id', $targetYearId)
        ->where('level_id', $assignment->class->level_id)
        ->where('series_id', $assignment->class->series_id)
        ->where('section', $assignment->class->section)
        ->first();

    if (!$targetClass) {
        $report['skipped'][] = ['assignment' => $assignment, 'reason' => 'Classe cible non trouvée'];
        continue;
    }

    $exists = TeacherSubjectAssignment::where([...unique fields...])->exists();
    if (!$exists) {
        TeacherSubjectAssignment::create([...]);
        $report['created']++;
    }
}
```

### Testing [Source: CLAUDE.md]
- Créer 2 années scolaires avec classes similaires
- Tester le mapping automatique

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Création initiale | SM Agent |
