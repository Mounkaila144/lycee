# Story 3.4: Duplication de la Configuration des Coefficients

## Status: Ready for Review

## Story

**As an** Admin,
**I want** dupliquer la configuration des coefficients d'un niveau/série vers un autre,
**so that** je puisse gagner du temps lors de la configuration initiale.

## Acceptance Criteria

1. Un bouton "Dupliquer cette configuration" est disponible sur l'écran de configuration des coefficients
2. Le clic ouvre un formulaire avec : Niveau cible (select), Série cible (select, si applicable)
3. Le système copie tous les coefficients du niveau/série source vers la cible
4. Si des coefficients existent déjà dans la cible, un message d'avertissement s'affiche
5. Options : "Remplacer tout", "Fusionner" (ajouter les manquants), "Annuler"
6. Après duplication, l'Admin est redirigé vers la configuration cible

## Tasks / Subtasks

- [x] Task 1: Créer l'endpoint de duplication (AC: 1-6)
  - [x] `POST /api/admin/subject-class-coefficients/duplicate`
  - [x] Body: source_level_id, source_series_id, target_level_id, target_series_id, strategy (replace|merge)

- [x] Task 2: Créer DuplicateCoefficientsRequest (AC: 2, 3)
  - [x] Validation: source et target level_id requis, series_id conditionnel
  - [x] Vérification que source != target

- [x] Task 3: Implémenter la logique de duplication (AC: 3, 4, 5)
  - [x] Strategy "replace": supprimer cible, copier source
  - [x] Strategy "merge": copier uniquement les manquants
  - [x] Retourner un rapport: created_count, skipped_count, replaced_count

- [x] Task 4: Tests (AC: 1-6)
  - [x] Test duplication replace
  - [x] Test duplication merge (pas de doublons)
  - [x] Test source vide
  - [x] Test duplication vers même cible (erreur)

## Dev Notes

### Dépendances
- Story 3.2

### Logique de Duplication
```php
// Strategy: replace
SubjectClassCoefficient::where('level_id', $targetLevel)
    ->where('series_id', $targetSeries)
    ->delete();

$source = SubjectClassCoefficient::where('level_id', $sourceLevel)
    ->where('series_id', $sourceSeries)
    ->get();

foreach ($source as $coeff) {
    SubjectClassCoefficient::create([
        'subject_id' => $coeff->subject_id,
        'level_id' => $targetLevel,
        'series_id' => $targetSeries,
        'coefficient' => $coeff->coefficient,
        'hours_per_week' => $coeff->hours_per_week,
    ]);
}
```

### API
```
POST /api/admin/subject-class-coefficients/duplicate
{
    "source_level_id": 6,
    "source_series_id": 1,
    "target_level_id": 7,
    "target_series_id": 2,
    "strategy": "replace"  // ou "merge"
}
```

### Testing [Source: CLAUDE.md]
- Tests: `tests/Feature/StructureAcademique/SubjectCoefficientApiTest.php`

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### File List
- `Modules/StructureAcademique/Http/Controllers/Admin/SubjectClassCoefficientController.php` (modified - added duplicate)
- `Modules/StructureAcademique/Http/Requests/DuplicateCoefficientsRequest.php` (new)
- `Modules/StructureAcademique/Routes/admin.php` (modified - added duplicate route)
- `tests/Feature/StructureAcademique/SubjectCoefficientApiTest.php` (modified - added duplicate tests)

### Completion Notes
- 4 new tests added (replace, merge, empty source, same target)
- Total 22 tests passing in SubjectCoefficientApiTest (62 assertions)
- Pint formatting: pass

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Création initiale | SM Agent |
| 2026-03-16 | 1.1 | Implementation complete - all tasks done | Dev Agent |
