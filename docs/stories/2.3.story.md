# Story 2.3: Création et Gestion des Classes

## Status: Aproved

## Story

**As an** Admin,
**I want** créer et gérer les classes scolaires de mon établissement,
**so that** je puisse définir les groupes d'élèves pour chaque niveau.

## Acceptance Criteria

1. Un écran "Gestion des Classes" affiche la liste des classes de l'année active
2. Je peux créer une classe avec :
   - Niveau (select dynamique : uniquement les niveaux des cycles actifs)
   - Série (select dynamique : apparaît uniquement si Niveau = 1ère ou Tle ; liste les séries actives)
   - Section/Lettre (texte libre, ex: A, B, 1, 2)
   - Capacité maximale (numérique, défaut 50)
   - Salle attitrée (texte, optionnel)
   - Professeur principal (select : enseignants disponibles)
3. Le nom complet est auto-généré : [Niveau + Série + Section] => "Tle C1", "6ème A", "2nde B"
4. Le nom complet doit être unique pour une même année scolaire
5. Le cycle est auto-déterminé selon le niveau choisi
6. Je peux modifier une classe existante (tous les champs modifiables sauf le niveau et la série si des élèves sont inscrits)
7. Je peux supprimer une classe si aucun élève n'y est inscrit
8. Si je tente de supprimer une classe avec élèves, un message d'erreur s'affiche
9. La liste des classes affiche : Nom complet, Cycle (badge couleur), Niveau, Série, PP, Effectif/Capacité, Salle, Actions
10. Le tableau est trié par défaut : Cycle, puis Niveau, puis Série, puis Section

## Tasks / Subtasks

- [x] Task 1: Créer la migration `classes` (AC: 2, 3, 4)
  - [x] Table: id, academic_year_id (FK), level_id (FK), series_id (FK nullable), section (nullable), name (string, auto-généré), max_capacity (smallint default 60), classroom (nullable), head_teacher_id (FK nullable -> users), timestamps, softDeletes
  - [x] UNIQUE(academic_year_id, name)

- [x] Task 2: Créer le model Classe (AC: 2-10)
  - [x] Connection tenant, SoftDeletes
  - [x] Relations: belongsTo(AcademicYear), belongsTo(Level), belongsTo(Series nullable), belongsTo(User, 'head_teacher_id')
  - [x] Accesseur: `full_name` auto-généré depuis level + series + section
  - [x] Boot method: auto-générer `name` avant save

- [x] Task 3: Créer les Form Requests (AC: 2, 3, 4, 6)
  - [x] StoreClasseRequest with series_id required for 1ERE/TLE
  - [x] UpdateClasseRequest

- [x] Task 4: Créer ClasseController (AC: 1-10)
  - [x] index(): Liste paginée, filtrée par cycle/niveau/série/année, tri par cycle>niveau>série>section
  - [x] store(): Créer classe avec auto-génération du nom
  - [x] show(): Détails avec relations eager loaded
  - [x] update(): Modifier avec vérifications
  - [x] destroy(): Vérifier absence d'élèves inscrits

- [x] Task 5: Créer ClasseResource (AC: 9)
  - [x] Retourner: id, name, level (whenLoaded), series (whenLoaded), section, max_capacity, classroom, head_teacher (whenLoaded), academic_year_id

- [x] Task 6: Routes, Factories, Tests (AC: tous)
  - [x] Route: CRUD complet
  - [x] ClasseFactory
  - [x] Tests: CRUD, auto-génération nom, unicité nom, blocage suppression, filtres

## Dev Notes

### Data Model [Source: docs/architecture/data-models.md#classes]
```sql
CREATE TABLE classes (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    academic_year_id BIGINT UNSIGNED NOT NULL,
    level_id BIGINT UNSIGNED NOT NULL,
    series_id BIGINT UNSIGNED NULL,
    name VARCHAR(50) NOT NULL,    -- auto-généré
    max_capacity SMALLINT UNSIGNED DEFAULT 60,
    head_teacher_id BIGINT UNSIGNED NULL,
    created_at, updated_at, deleted_at,
    UNIQUE KEY (academic_year_id, name),
    FK level_id -> levels(id) ON DELETE RESTRICT,
    FK series_id -> series(id) ON DELETE SET NULL,
    FK head_teacher_id -> users(id) ON DELETE SET NULL
);
```

### Auto-génération du nom [Source: docs/prd/module-structure-academique.md#FR14]
Règle: `{Niveau} {Série}{Section}`
Exemples:
- Niveau=6E, Section=A → "6ème A"
- Niveau=TLE, Série=C, Section=1 → "Tle C1"
- Niveau=2NDE, Section=B → "2nde B"
- Niveau=1ERE, Série=A, Section=null → "1ère A"

Mapping codes niveaux → noms affichés:
| Code | Nom affiché |
|------|-------------|
| 6E | 6ème |
| 5E | 5ème |
| 4E | 4ème |
| 3E | 3ème |
| 2NDE | 2nde |
| 1ERE | 1ère |
| TLE | Tle |

Ajouter un champ `section` (nullable) pour la lettre de section.

### Champ `classroom`
Ajout d'un champ texte optionnel `classroom` (salle attitrée) non présent dans le data-model architecture mais requis par le PRD (FR13). L'ajouter dans la migration.

### API Endpoints [Source: docs/architecture/external-apis.md]
```
GET    /api/admin/classes
POST   /api/admin/classes
GET    /api/admin/classes/{id}
PUT    /api/admin/classes/{id}
DELETE /api/admin/classes/{id}
```

### Filtres index()
Query params: `?cycle_id=&level_id=&series_id=&academic_year_id=`
Tri par défaut: level.order_index ASC, series.code ASC, section ASC

### Testing [Source: CLAUDE.md]
- Tests: `tests/Feature/StructureAcademique/ClasseApiTest.php`
- Créer Level et Series via factories/seeders dans setUp
- Tester auto-génération nom complet
- Tester unicité nom par année scolaire
- Tester série obligatoire pour 1ERE et TLE

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### File List
- `Modules/StructureAcademique/Database/Migrations/tenant/2026_01_10_130004_create_classes_table.php` (new)
- `Modules/StructureAcademique/Entities/Classe.php` (new)
- `Modules/StructureAcademique/Http/Controllers/Admin/ClasseController.php` (new)
- `Modules/StructureAcademique/Http/Resources/ClasseResource.php` (new)
- `Modules/StructureAcademique/Http/Requests/StoreClasseRequest.php` (new)
- `Modules/StructureAcademique/Http/Requests/UpdateClasseRequest.php` (new)
- `Modules/StructureAcademique/Database/Factories/ClasseFactory.php` (new)
- `Modules/StructureAcademique/Routes/admin.php` (modified)
- `tests/Feature/StructureAcademique/ClasseApiTest.php` (new)

### Completion Notes
- All 17 tests passing (55 assertions)
- Name auto-generation tested for all 7 level codes
- Series required validation for 1ERE and TLE
- Unique name per academic year enforced

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Création initiale | SM Agent |
| 2026-03-16 | 1.1 | Implementation complete - all tasks done | Dev Agent |
