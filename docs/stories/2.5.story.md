# Story 2.5: Filtrage et Navigation des Classes

## Status: Aproved

## Story

**As an** Admin,
**I want** filtrer et naviguer dans la liste des classes selon différents critères,
**so that** je puisse retrouver rapidement une classe spécifique.

## Acceptance Criteria

1. Des filtres sont disponibles au-dessus de la liste des classes :
   - Cycle (select : Tous, Collège, Lycée)
   - Niveau (select : Tous, 6e, 5e, ..., Tle)
   - Série (select : Toutes, A, C, D - visible uniquement si Lycée sélectionné)
   - Année scolaire (select, défaut : année active)
2. Les filtres sont cumulables (AND logic)
3. Un bouton "Réinitialiser filtres" efface tous les filtres
4. Le nombre de classes affichées est visible (ex: "12 classes trouvées")
5. Un compteur global affiche : "Total élèves : X dans Y classes"

## Tasks / Subtasks

- [x] Task 1: Améliorer ClasseController.index() (AC: 1, 2, 4, 5)
  - [x] Query params: cycle_id, level_id, series_id, academic_year_id, search
  - [x] Defaults to active year when no academic_year_id provided
  - [x] Filtrage via `when()` clauses

- [x] Task 2: Ajouter un endpoint stats globales (AC: 5)
  - [x] `GET /api/admin/classes/stats?academic_year_id=X`
  - [x] Retourner: total_classes, classes_by_cycle, classes_by_level

- [x] Task 3: Tests filtres (AC: 1-5)
  - [x] Test filtrage par cycle (collège, lycée)
  - [x] Test filtrage par niveau
  - [x] Test filtrage par série
  - [x] Test filtrage combiné cycle + niveau
  - [x] Test filtrage combiné cycle + niveau + série
  - [x] Test filtre par année scolaire
  - [x] Test défaut année active
  - [x] Test recherche par nom
  - [x] Test stats endpoint (total, par cycle, par niveau)

## Dev Notes

### Dépendances
- Story 2.3 (ClasseController et routes existants)

### Filtrage Query Params
```
GET /api/admin/classes?cycle_id=1&level_id=3&series_id=2&academic_year_id=5&per_page=25
```

### Implémentation Filtres
```php
Classe::query()
    ->when($request->cycle_id, fn($q, $cycleId) =>
        $q->whereHas('level', fn($q2) => $q2->where('cycle_id', $cycleId))
    )
    ->when($request->level_id, fn($q, $levelId) => $q->where('level_id', $levelId))
    ->when($request->series_id, fn($q, $seriesId) => $q->where('series_id', $seriesId))
    ->when($request->academic_year_id,
        fn($q, $yearId) => $q->where('academic_year_id', $yearId),
        fn($q) => $q->whereHas('academicYear', fn($q2) => $q2->where('is_active', true))
    )
```

### Testing [Source: CLAUDE.md]
- Pattern: Bearer token + InteractsWithTenancy
- Créer plusieurs classes avec différents cycles/niveaux/séries pour tester les filtres

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### File List
- `Modules/StructureAcademique/Http/Controllers/Admin/ClasseController.php` (modified - added stats endpoint)
- `Modules/StructureAcademique/Routes/admin.php` (modified - added stats route)
- `tests/Feature/StructureAcademique/ClasseFilterTest.php` (new)

### Completion Notes
- All 14 tests passing (35 assertions)
- Filters already built in Story 2.3, enhanced with stats endpoint
- Stats include total_classes, classes_by_cycle, classes_by_level

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Création initiale | SM Agent |
| 2026-03-16 | 1.1 | Implementation complete - all tasks done | Dev Agent |
