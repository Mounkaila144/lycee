# Story 1.2: Activation d'une Année Scolaire

## Status: Ready for Review

## Story

**As an** Admin,
**I want** définir une année scolaire comme "active",
**so that** tous les formulaires utilisent cette année par défaut et que le système sache quelle est l'année en cours.

## Acceptance Criteria

1. Un bouton "Activer" est disponible sur chaque année scolaire non active
2. Le clic sur "Activer" désactive l'année précédemment active et active la nouvelle
3. Une seule année scolaire peut être active à la fois
4. Un message de confirmation s'affiche : "Voulez-vous activer l'année scolaire 2025-2026 ? L'année 2024-2025 sera désactivée."
5. Après activation, tous les formulaires (classes, inscriptions, affectations) utilisent cette année par défaut
6. L'année active est identifiable visuellement dans la liste (badge vert, mise en évidence)
7. Il n'est pas possible de désactiver une année sans en activer une autre

## Tasks / Subtasks

- [x] Task 1: Ajouter l'endpoint d'activation (AC: 1, 2, 3)
  - [x] Route: `POST /api/admin/academic-years/{id}/activate`
  - [x] Dans AcademicYearController: méthode `activate(AcademicYear $academicYear)`
  - [x] Logique: désactiver toutes les années, activer celle demandée (transaction DB)

- [x] Task 2: Ajouter la route dans admin.php (AC: 1)
  - [x] `Route::post('academic-years/{academic_year}/activate', [AcademicYearController::class, 'activate'])`

- [x] Task 3: Ajouter le champ is_active dans AcademicYearResource (AC: 5, 6)
  - [x] S'assurer que is_active est retourné dans la resource
  - [x] Ajouter un endpoint `GET /api/admin/academic-years/active` pour obtenir l'année active

- [x] Task 4: Écrire les tests (AC: 1-7)
  - [x] Test activation réussie
  - [x] Test désactivation automatique de l'année précédente
  - [x] Test unicité de l'année active (une seule à la fois)
  - [x] Test activation d'une année déjà active (idempotent)

## Dev Notes

### Dépendances
- Requiert Story 1.1 (AcademicYear model et controller existants)

### API Endpoint [Source: docs/architecture/external-apis.md]
```
POST /api/admin/academic-years/{id}/activate
```
Réponse: `{ message: "Année scolaire 2025-2026 activée avec succès.", data: AcademicYearResource }`

### Logique Métier
L'activation doit être atomique (transaction):
```php
DB::transaction(function () use ($academicYear) {
    AcademicYear::where('is_active', true)->update(['is_active' => false]);
    $academicYear->update(['is_active' => true]);
});
```

### Endpoint supplémentaire utile
`GET /api/admin/academic-years/active` - Retourner l'année active courante (utilisé par d'autres modules pour pré-remplir les formulaires).

### Testing [Source: CLAUDE.md]
- Location: `tests/Feature/StructureAcademique/AcademicYearApiTest.php` (ajouter aux tests existants)
- Pattern: Bearer token + InteractsWithTenancy
- Tester la transactionalité (pas de double active)

## File List

### Modified Files
- `Modules/StructureAcademique/Http/Controllers/Admin/AcademicYearController.php` - Méthodes activate() et active()
- `Modules/StructureAcademique/Routes/admin.php` - Routes activate et active
- `Modules/StructureAcademique/Http/Resources/AcademicYearResource.php` - Champ is_active inclus
- `tests/Feature/StructureAcademique/AcademicYearApiTest.php` - Tests activation (5 tests)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Création initiale | SM Agent |
| 2026-03-16 | 1.1 | Implémentation complète - Endpoint activation, désactivation auto, endpoint active year, 5 tests passing | Dev Agent |
