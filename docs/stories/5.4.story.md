# Story 5.4: Barèmes par Cycle (optionnel)

## Status: Aproved

## Story

**As an** Admin,
**I want** configurer des barèmes différents pour le collège et le lycée,
**so that** je puisse appliquer des critères adaptés à chaque cycle si nécessaire.

## Acceptance Criteria

1. Un toggle "Barèmes différenciés par cycle" est disponible dans la configuration
2. Si activé, deux onglets "Collège" et "Lycée" apparaissent, chacun avec ses propres paramètres
3. Si désactivé, un seul jeu de paramètres s'applique à tout l'établissement
4. Les barèmes par cycle sont enregistrés dans la table `class_settings` avec le `cycle_id` correspondant
5. Les barèmes sans `cycle_id` (null) s'appliquent globalement

## Tasks / Subtasks

- [ ] Task 1: Ajouter la logique de barèmes par cycle (AC: 1-5)
  - [ ] Le model ClassSetting supporte déjà cycle_id nullable
  - [ ] Quand différencié: créer un enregistrement par cycle
  - [ ] Quand global: utiliser l'enregistrement avec cycle_id=null

- [ ] Task 2: Ajouter endpoint pour activer/désactiver la différenciation (AC: 1)
  - [ ] `POST /api/admin/class-settings/toggle-cycle-differentiation`
  - [ ] Si activation: dupliquer le barème global vers chaque cycle
  - [ ] Si désactivation: supprimer les barèmes par cycle, garder le global

- [ ] Task 3: Modifier ClassSettingController (AC: 2, 3)
  - [ ] show(): Retourner barème global OU barèmes par cycle selon le mode
  - [ ] update(): Accepter cycle_id optionnel dans le body

- [ ] Task 4: Tests (AC: 1-5)
  - [ ] Test activation différenciation
  - [ ] Test désactivation
  - [ ] Test modification barème par cycle
  - [ ] Test lecture barèmes différenciés

## Dev Notes

### Dépendances
- Story 5.1, Story 2.1 (Cycles)

### Logique de Différenciation
```php
// Activation
public function toggleCycleDifferentiation(Request $request): JsonResponse
{
    $enable = $request->boolean('enable');

    if ($enable) {
        $globalSetting = ClassSetting::whereNull('cycle_id')->first();
        $cycles = Cycle::active()->get();
        foreach ($cycles as $cycle) {
            ClassSetting::firstOrCreate(
                ['cycle_id' => $cycle->id, 'academic_year_id' => null],
                $globalSetting->only([...threshold fields...])
            );
        }
    } else {
        ClassSetting::whereNotNull('cycle_id')->delete();
    }
}
```

### Résolution du Barème
Quand un module (Notes, Bulletins) a besoin du barème pour une classe:
1. Chercher ClassSetting avec cycle_id = cycle de la classe
2. Si pas trouvé, utiliser ClassSetting avec cycle_id = null (global)

### Testing [Source: CLAUDE.md]
- Tests: `tests/Feature/StructureAcademique/ClassSettingApiTest.php`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Création initiale | SM Agent |
